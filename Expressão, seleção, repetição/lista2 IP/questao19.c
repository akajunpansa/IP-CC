#include <stdio.h>
int main () {
    float a, b, c, d;

    scanf("%f %f %f %f", &a, &b, &c, &d);
    
    if(a>b && b>c && c>d);
        printf("%f %f %f %f", d, c, b, a);
}   else if(b>a && a>c && c>d);
        printf("%f %f %f %f", d, c, b, a);co
    return 0;
}